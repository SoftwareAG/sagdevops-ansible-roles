---

    - import_role:
        name: configure-integrationserver
      vars:
        configs_action: set-jvm-memory
        is_jvm_heap_min: "{{ process_memory_min }}"
        is_jvm_heap_max: "{{ process_memory_max }}"