---

  - name: "Update config file {{ custom_wrapper_file }} with ids_jvm_params params if defined"
    lineinfile:
      path: "{{ custom_wrapper_file }}"
      regexp: '^{{ item.key }}'
      insertafter: '^#{{ item.key }}'
      line: "{{ item.key }}={{ item.value }}"
    with_dict: "{{ ids_jvm_params }}"
    when: ids_jvm_params is defined and (ids_jvm_params|length > 0)