---

- name: Execute encrypt password
  include_tasks: "tenantmgt/encrypt_password.yml"

- name: Print the cmd_result to the console.
  debug:
    msg: "{{ cmd_result }}"

- name: extract encrypted password
  shell: "echo {{ cmd_result.stdout_lines }}"
  register: result

- name: Print the result to the console.
  debug:
    msg: "{{ result }}"