---

- import_tasks: "command_{{ product_command }}.yaml"
  vars:
    service_exec_filepath: "{{ webmethods_install_dir }}/InternalDataStore/bin/el_initd"
    service_name: "sag1cel{{ webmethods_install_major_version }}"
    # service_env: "{{ service_env_internaldatastore_defaults }}"