---

  - name: Set changed flag to false if not defined
    set_fact: 
      configure_integrationserver_wxconfig_set_extended_settings_changed: false
    when: configure_integrationserver_wxconfig_set_extended_settings_changed is not defined

  - name: Install the wxconfig configs in IS target package only if value variable is not empty
    block:

      - name: fail if wxconfig_target_package_name not specified
        fail: 
          msg: "wxconfig_target_package_name not specified...cannot create."
        when: wxconfig_target_package_name is not defined or wxconfig_target_package_name == ""

      - name: Check if main wxconfig controller file is there already
        stat: 
          path: "{{ wxconfig_target_package_config_path }}/wxconfig.cnf"
        register: stat_wxconfig_enabled

      - name: fail if wxconfig is not enabled via the controller file
        fail: 
          msg: "wxconfig controller file wxconfig.cnf is not present in current package {{ wxconfig_target_package_name }}...make sure to enable WxConfig for this package"
        when: stat_wxconfig_enabled.stat.exists == false

      - name: Populate wxconfig file if the wxconfig_extended_settings_list is defined and non empty
        template:
          src: as-extendedSettings-template.xml
          dest: "{{ wxconfig_target_package_config_path }}/as-extendedSettings.xml"
        register: "result_wxconfig_content_operation"

      - name: Enable this new config in the main controller file
        lineinfile:
          path: "{{ wxconfig_target_package_config_path }}/wxconfig.cnf"
          regexp: '^{{ item }}'
          insertafter: '^#{{ item }}'
          line: "{{ item }}"
        register: "result_wxconfig_controller_operation"
        with_items:
          - "wx.config.incl=as-extendedSettings.xml"

      - name: Set changed flag to true if any mutating operation did change
        set_fact: 
          configure_integrationserver_wxconfig_set_extended_settings_changed: true
        when: result_wxconfig_content_operation.changed == true or result_wxconfig_controller_operation.changed == true

    when: wxconfig_extended_settings_list is defined and wxconfig_extended_settings_list|length > 0